import React from 'react';
import ReactDom from 'react-dom';
import SearchBar from 'search-bar.js';
import VideoScreen from 'video-screen.js';
import VideoList from 'video-list.js';
import YoutubeAPIKey from 'youtubeapikey';



/*
//code for search-bar.js
//this.state for initial state
//this.setState to change state always

class SearchBar extends Component{
  constructor(props){
    super(props);
    this.state = {
      initialState: "React video tutorial";
      search: " "
    }
  }
  
  changeSearch(search){
    this.setState({search});
  }
  
  
  
}
*/